# 🐧 Aashutosh's Neovim Config (NvChad v2.5)

A highly customized, memory-efficient Neovim environment built on top of [NvChad](https://nvchad.com/). Optimized for **Data Science**, **Infrastructure**, and cross-platform performance between **macOS (M2 Air)** and **Linux**.

## 🚀 Features

- **Portability:** Designed to sync seamlessly across macOS and Linux.
- **Languages:** Configured for Python, Rust, Go, R, C, and C++.
- **File Management:** - `nvim-tree` for a clean sidebar view (dotfiles hidden by default).
  - `oil.nvim` for editing the file system like a buffer.
- **UI & UX:** `noice.nvim` for modern notifications and `undotree` for visual history management.
- **AI Integration:** Integrated with GitHub Copilot.

## 🛠 Installation

### 1. Prerequisites
Ensure you have the following installed on your system:
- **Neovim (v0.10+ recommended)**
- **Nerd Font:** (JetBrainsMono or FiraCode recommended for Ghostty)
- **Ripgrep:** Required for Telescope live grep.
- **System Dependencies:** `gcc`, `make`, `unzip`, `nodejs/npm`.

### 2. Setup
Clone this repository into your Neovim configuration directory:

```bash
git clone [https://github.com/YOUR_USERNAME/nvim-config.git](https://github.com/YOUR_USERNAME/nvim-config.git) ~/.config/nvim
